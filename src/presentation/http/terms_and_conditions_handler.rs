//! TermsAndConditions REST handlers
//!
//! Auto-generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{TermsAndConditionsService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateTermsAndConditionsDto, UpdateTermsAndConditionsDto, PatchTermsAndConditionsDto, TermsAndConditionsResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum TermsAndConditionsError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for TermsAndConditionsError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for TermsAndConditionsError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "TERMSANDCONDITIONS_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "TERMSANDCONDITIONS_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "TERMSANDCONDITIONS_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "TERMSANDCONDITIONS_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for TermsAndConditions.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /terms_and_conditionses | List with pagination |
/// | POST | /terms_and_conditionses | Create new |
/// | GET | /terms_and_conditionses/:id | Get by ID |
/// | PUT | /terms_and_conditionses/:id | Full update |
/// | PATCH | /terms_and_conditionses/:id | Partial update |
/// | DELETE | /terms_and_conditionses/:id | Soft delete |
/// | POST | /terms_and_conditionses/bulk | Bulk create |
/// | POST | /terms_and_conditionses/upsert | Upsert |
/// | GET | /terms_and_conditionses/trash | List deleted |
/// | POST | /terms_and_conditionses/:id/restore | Restore |
/// | DELETE | /terms_and_conditionses/empty | Empty trash |
/// | GET | /terms_and_conditionses/:id/deleted | Get deleted by ID |
/// | DELETE | /terms_and_conditionses/trash/:id | Permanent delete from trash |
/// | GET | /terms_and_conditionses/count | Count active entities |
/// | GET | /terms_and_conditionses/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(TermsAndConditionsService::with_repository(repository));
/// let router = create_terms_and_conditions_routes(service);
/// ```
pub fn create_terms_and_conditions_routes(service: Arc<TermsAndConditionsService>) -> Router {
    BackboneCrudHandler::<TermsAndConditionsService, TermsAndConditions, CreateTermsAndConditionsDto, UpdateTermsAndConditionsDto, TermsAndConditionsResponseDto>::routes(
        service,
        "/terms_and_conditionses",
    )
}

/// Create Axum router with only the read (GET) endpoints for TermsAndConditions.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_terms_and_conditions_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_terms_and_conditions_read_routes(service: Arc<TermsAndConditionsService>) -> Router {
    BackboneCrudHandler::<TermsAndConditionsService, TermsAndConditions, CreateTermsAndConditionsDto, UpdateTermsAndConditionsDto, TermsAndConditionsResponseDto>::read_routes(
        service,
        "/terms_and_conditionses",
    )
}

/// Create Axum router with only the write (mutation) endpoints for TermsAndConditions.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_terms_and_conditions_write_routes(service: Arc<TermsAndConditionsService>) -> Router {
    BackboneCrudHandler::<TermsAndConditionsService, TermsAndConditions, CreateTermsAndConditionsDto, UpdateTermsAndConditionsDto, TermsAndConditionsResponseDto>::write_routes(
        service,
        "/terms_and_conditionses",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_terms_and_conditions_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<TermsAndConditionsService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_terms_and_conditions_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

